# Supabase PostgreSQL (Project: wlaagpzbwxjclcjagfvr)
# Dashboard → Project Settings → Database → Connection string → URI
# Use Session pooler URI on Render (IPv4). Direct host may fail from cloud hosts.
DATABASE_URL="postgresql://postgres.wlaagpzbwxjclcjagfvr:YOUR_PASSWORD@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"
PORT=3001
# Used only to backfill passwordHash for existing users missing one (first deploy after upgrade)
DEMO_PASSWORD="shayan123"
# Comma-separated allowed frontend origins for CORS
CORS_ORIGINS="http://localhost:5173,http://localhost:5174"
