# Set to true to use the Express + SQLite backend (via Vite proxy to :3001)
VITE_USE_API=true
# Optional override — leave empty to use same-origin /api proxy
VITE_API_URL=https://shayan-banquet-api.onrender.com
